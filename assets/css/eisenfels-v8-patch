
/* Eisenfels V8: Termin-/Ausbildungskarten */
.event-card h3{margin-bottom:10px}
.details-btn{
  display:inline-block;
  margin-top:8px;
  padding:9px 12px;
  border-radius:12px;
  background:linear-gradient(135deg,var(--blue),var(--orange));
  color:white!important;
  text-decoration:none;
  font-weight:800;
}
.details-btn:hover{filter:brightness(1.12)}
